function main() {
    // initialize our canvas context

    /*
     var canvasMain = document.createElement("canvas");
     canvasMain.width = GLOBAL.gameWidth;
     canvasMain.height = GLOBAL.gameHeight;
     var contextMain = canvasMain.getContext('2d');
     var canvasState = new CanvasState(canvasMain);
     */

    // create canvas state object
    // create deck
    // create cards
    // shuffle deck
    // deal from deck and show on UI
    // show remaining deck on UI
    // handle events
    //// click on card
    //// click on deck

    GLOBAL.game = new Game();
    GLOBAL.game.run();

}